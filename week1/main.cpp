// carterGrace coding 1
// varibles and logic

// compile with the developer command prompt
// 'cl /EHsc main.cpp
// 'main' or 'main.exe'

#include <iostream>
using namespace std;

int main() {
    cout << "lets learn about varibles and logic!\n";

    // declaring varibles
    int playerAge = -1;
    string playerName = "Darth Vader";
    float happinessPrecent = 0.61f;
    bool keepPlaying = true;

    // just like branch
    // if true run code
    if(player == -1){
        cout << "how old are you?\n";
        cin >> playerAge;

        if(playerAge <= 12) {
            cout << "im sorry, this game is for grownups!\n";
            return 0;
        }
        else{
            cout << "wow, " <<playerAge <<
        }
    }
}